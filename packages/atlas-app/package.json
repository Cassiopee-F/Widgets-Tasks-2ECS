{
  "name": "atlas-app",
  "version": "1.0.0",
  "private": true,
  "description": "Atlas — application de terrain (Android). Enveloppe Capacitor autour du meme code que le widget.",
  "scripts": {
    "vendoriser": "node scripts/vendoriser.mjs",
    "sync": "npm run vendoriser && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
